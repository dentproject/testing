name: Pythonlint

on:
  pull_request:
  push:
  workflow_dispatch:

jobs:
  python-isort:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: ricardochaves/python-lint@v1.4.0
        with:
          python-root-list: "DentOS_Framework"
          use-pylint: false
          use-pycodestyle: false
          use-flake8: false
          use-black: false
          use-mypy: false
          use-isort: true
          extra-isort-options: "--line-length 160 --wrap-length 80 --multi-line 3 --project dent_os_testbed --project gen --trailing-comma --force-grid-wrap 2"
  